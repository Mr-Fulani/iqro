# IQRO mobile — следующий этап публичного выпуска

Статус: в работе. Наличие нового интерфейса не означает готовность публикации.
Каждый законченный этап: анализ, профильные тесты, проверка Android, commit.

1. Исправить ощутимый отклик при перелистывании/перетягивании страниц, сохранить системный
   opt-out и добавить настройку приложения. Не менять настройки Android автоматически.
2. Инвентаризировать Мусхафы backend/Quran.Foundation/quran.com/JMApps. Подключать только
   проверенные страницы конкретного издания, не смешивать риваяты/нумерацию/аудио.
   Для каждого нового источника фиксировать происхождение, условия, полный набор страниц,
   геометрию аятов, checksum, размер и результат проверки. Не обозначать metadata как готовое издание.
3. Календарь Хиджры: offline-friendly расчёт с явно указанным методом и поправкой,
   календарная сетка, источники значимых дат, оговорка о местном наблюдении луны.
4. Главная IQRO: единый мотив света/арки, читаемая иерархия, действующие переходы,
   календарь и продолжение чтения без декоративных заглушек, RU/EN/AR/TR и крупный текст.
5. Единое качественное оформление портретов и лёгкая анимация существующего знака.
   Загруженные администратором изображения не менять и не перезаписывать.
   Не генерировать лица реальных чтецов. Respect reduced motion, без вечных анимаций/blur.
6. Release gate: полный анализ/тесты, offline/повороты/аудио/слабый Android, доступность,
   подтверждение контента, production URL, signing, privacy/store forms. Публикацию в магазины
   не выполнять без отдельного согласования. Не объявлять неподтверждённые проверки зелёными.

Дисковое пространство не освобождать удалением без отдельного точного разрешения пользователя.

### 2026-09-08 — Android Tajweed QA и непрерывное изображение при повороте

- На Redmi Note 7 открыто `qcf-v4-tajweed-hafs`, страница 51. Сохранённый аят
  3:11 выделен корректно; короткий тап показывает меню/круглую кнопку плеера,
  длинный — цветной фрагмент того же издания, перевод и тафсир.
- Запуск из круглой кнопки: Android media session `Аль Имран · 11`, Saad al-Ghamdi,
  состояние playing, затем paused на 16126 ms, `error=null`. На экране правильное
  выделение и администраторский портрет. Это проверка воспроизведения программно,
  не подтверждение слышимости динамика со стороны пользователя.
- Первый landscape после 3 s показал временно пустое изображение при переходе на
  другой размер; после ожидания загружено чёткое цветное изображение, возврат в
  portrait сохраняет 3:11/p51. Исправлены обе причины мигания: `Expanded` области
  страницы имеет стабильный key при появлении/скрытии заголовка; `MushafRaster`
  сохраняет один декодированный кадр до готовности нового размера той же страницы.
  Identity включает edition/version/page; FutureBuilder разделён этой же identity,
  чтобы не показывать старое издание под новой геометрией. Excerpts не включают
  retention до проверки собственных областей. Дополнительного дискового кеша нет.
- 8 новых widget tests: реальный rebuild portrait/landscape/portrait, задержка и
  ошибка нового decode, изоляция издания/версии/страницы, поздний obsolete stream.
  **Полный Flutter suite: 379 passed / 13 opt-in skipped; analyze: no issues.**
- Полный Tajweed offline download начат штатно после подтверждения 245 MiB.
  При 296/604 (116 MiB) загрузка прервалась; причина пока не установлена. Есть
  штатное продолжение с сохранённого места, прочие пакеты не удалялись. Завершение
  604/604 и cold-start без сети на этом checkpoint ещё не подтверждены.
- Исходные настройки поворота Android восстановлены: accelerometer_rotation=1,
  user_rotation=0. Батарея 100%, температура в ходе проверки 37–41 °C. Новая
  установка исправления и повторная device QA фиксируются отдельным результатом.

### 2026-09-08 — календарь и Tajweed опубликованы, APK обновлён

- Код зафиксирован этапами: `5d04dcb` общий календарь и
  **`c909c72e68df2f6b945c2e7c48bb15ebcb58ab80`** QCF V4 Tajweed. Именно c909c72
  доставлен чистым `git archive` на staging после verified backup. Budget build/up,
  healthchecks, миграции calendar 0001/0002 и runtime limits прошли; оба системных
  таймера active. Секреты и volumes не заменялись. После сборки свободно около 1.5 GiB.
- `https://staging.iqro.forum/ru/calendar`: живой календарь; проверены RU/EN/AR/TR,
  текущий месяц, отметка белых дней, пояснение/источник. Общий API отдаёт 8 событий
  версии `f6ea92f74d97bb8b381ca3cc8851dc0167de547bff614209a622f7d9b461fb80`.
  `/admin/calendar/calendarevent/` доступен через штатную авторизацию. В browser QA
  не было авторизованной admin-сессии; вёрстка/валидация формы проверены backend-тестом,
  не выдавать страницу входа за проверку сохранения записи на live сервере.
- **QCF V4 · Таджвид** / `qcf-v4-tajweed-hafs@qcf-v4-tajweed-iqro-20260908-v1`
  опубликован штатным publisher: 604 страницы. Все 2418 файлов (476.5 MiB, включая
  manifest/геометрию/три ширины) сверены SHA на staging. Передача SSH заняла почти
  3 часа; один оборванный фрагмент повторён по checksum. До завершения публикация
  не включалась. Одноразовое RAM-задание завершено штатно.
- Public CDN smoke: страницы 1/2/3/27/51/208/604, все 720/1440/2160 px, bytes/SHA
  совпали. RU/EN/AR/TR native catalog и полный offline manifest прошли. Пакет 2160 px:
  **256491814 bytes (~245 MiB)**, checksum
  `3a7202365e2ae53e4a0cdc428cec9f88ab9dfce664e8d11bfcadb1e95d20670f`.
  Отчёт: `tmp/mushaf-tajweed-20260908/live-verification.json`.
- Полные read-only fingerprints после deploy и после publication **совпали с baseline**:
  18 чтецов, 36 recitation editions, 4104 tracks, 12472 ayahs и все 6236 canonical refs.
  Сохранены прежние KFGQPC/QCF V2/Madani и администраторские портреты.
- Итоговые local gates: **787 backend passed / 7 skipped; 371 Flutter passed /
  13 opt-in skipped**, analyzers чистые, 5 web E2E и production build прошли.
  Дополнительно source tests (4), backend full publication (1) и Flutter full corpus
  (1) прошли с реальными 604 страницами. Новые provider tests (3) подтверждают
  быстрый seed, обновление/кеширование, сохранение пустого каталога при offline и retry.
- Android arm64 profile staging APK **установлен через `adb install -r`, Success**,
  58376892 bytes, SHA256
  `1913eefb1e1961aa2de18d14cab756bfd57a0dae935214a0c8d67af42cceac51`.
  Сохранены аккаунт, данные, прогресс 3:11/p51, чтец/портрет Саада аль-Гамди и пауза
  плеера. Проверены главная, сетка календаря, выделение 13-го дня и диалог пояснения
  с источником. Скриншоты: `tmp/calendar-tajweed-staging-20260908/android-calendar*.png`.
- **Нативный QCF V4 device/offline QA ещё не подтверждён на этом checkpoint.** После
  сна телефон показал lock screen, последующие USB screencap вернули пустые файлы.
  Запасная запись снимка на `/sdcard` заблокирована проверкой разрешений и не выполнена.
  Пользователю предложено разблокировать телефон. Последний read-only статус: питание
  есть, 100%, 37 °C. Не менять настройки блокировки и не очищать данные ради QA.

Production/store публикация не выполнялась: полный редакционный и device sign-off
остаётся отдельным release gate, даже при зелёной технической проверке корпуса.

### 2026-09-08 — общий календарь и полный QF19 Tajweed: код и local gates

- Реализованы backend/admin/API событий календаря и веб `/{locale}/calendar`.
  Flutter использует общий каталог с persistent offline fallback, без изменения
  локального метода Умм аль-Кура. Четыре языка, источники, автоматический код события,
  проверка полного перевода перед публикацией. Инструкция: `HIJRI_CALENDAR.md`.
- Полный source19 QCF V4 Tajweed: 604 страницы, 6236 canonical references,
  1812 lossless WebP, исходные цветные слои и единая IQRO grid. Manifest
  `8c9bbab040e59a5000a2c443f2f416b61a0ecad029eec0ad7b3ad8bb0c59c68d`.
  Все source tests, backend full publication в изолированной тестовой БД и Flutter
  all-pages/all-widths decode/hit-test прошли. `ops/mushaf-glyph-pilot/TAJWEED.md`.
- Backend full suite: 786 passed / 7 opt-in skipped, mypy/ruff чистые; затем добавлен
  отдельный admin-form test, все 19 calendar tests прошли. OpenAPI validation: 0 errors,
  4 предупреждения enum naming существующих API; строгий fail-on-warn не зелёный.
- Web typecheck/lint/production build успешны; 5 browser tests RU/EN/AR/TR + retry
  прошли. Во время локального Next build upstream demo API не отдавал часть Dua
  endpoints (404), сработал существующий fallback; это не live content smoke.
- Пользователь разрешил ровно 8 obsolete backend images. Они удалены по точным
  SHA, без force/prune, свободно около 3 GiB; текущие и два fallback releases оставлены.
  Никакое дальнейшее освобождение места не разрешено этим согласием.
- Fresh backup `quran_staging_20260908T070402Z.dump`: offsite full download verification
  прошла после восстановления SSH. Baseline: 18 Reciter / 36 editions / 4104 tracks /
  12472 Ayah, canonical `01a06fb5-19b2-7465-af74-4b579e50c7be`; hashes совпадают с предыдущим.
- Телефон вновь подключён: Redmi Note 7 `91aedea7`, батарея 100%, 33 °C, питание есть,
  /data свободно 1.3 GiB. Новая установка и live публикация на этом checkpoint ещё
  не выполнены: последующий итог дописывается только после фактического завершения.

### 2026-09-08 — доступ восстановлен; оба полных typography releases опубликованы

- После возвращения пользователем прежнего IP SSH восстановился. Runtime остаётся
  `5b4ae94`; новые образы, миграции и `.deployed-commit` не менялись. Перед публикацией
  повторный read-only audit совпал с baseline, проверенный private/offsite backup
  `quran_staging_20260907T220911Z.dump` сохранён (подробности ниже).
- Включены **`kfgqpc-iqro-20260908-v2`** и **`qcf-v2-iqro-20260908-v4`**:
  по 604 страницы, 1812 lossless WebP (720/1440/2160) на издание. SHA manifest
  совпадает с локальными release receipts ниже. Старые immutable версии сохранены.
- Передача по SSH была крайне медленной (около 30–60 KiB/s) и прерывалась.
  Неполные данные не публиковались. Доставка завершена через один RAM-контейнер
  на издание, последовательно, шестью потоками по ~2 MiB с повтором только
  несовпавших файлов. Каждый из 2418 файлов повторно проверен по SHA на сервере;
  штатные `--validate-only` и `publish_mushaf_rendition --upload-workers 4` прошли.
  Одноразовые RAM-задания завершены; persistent media, backups и caches не удалялись.
  Helper: `tmp/mushaf-typography-20260908/publish_parallel_staging.py`.
- Альтернативная прямая HTTPS-загрузка по временной подписанной ссылке получила
  `403 AccessDenied`, поэтому не использовалась для публикации. Ключи оставались
  на сервере, ссылки не выводились/не сохранялись; права, firewall и secrets не менялись.
  Штатная серверная immutable-загрузка одной контрольной страницы прошла и затем
  была переиспользована полным publisher. Причину внешнего AccessDenied не объявлять
  установленной. Python urllib также получил 403 при проверке публичного CDN;
  повтор стандартным curl и фактические SHA-проверки прошли.
- **Public smoke PASS**: RU/EN/AR/TR catalog с обоими новыми releases; offline manifests
  по 604 страницы, width 2160; страницы 1/2/3/50/77/255/604 во всех трёх ширинах
  скачаны через CDN и сверены по bytes/SHA (42 файла). При первом запросе после
  QCF publication каталог ещё отдавал прежнюю версию из штатного 300s edge cache;
  повтор подтвердил новые версии без purge. Отчёт: `live-verification.json` в том же tmp.
  Offline bytes: QCF **206527268**, KFGQPC **205607322**.
- После обеих публикаций полный fingerprint снова совпал: **18 Reciter, 36
  RecitationEdition, 4104 AudioTrack, 1 QuranEdition, 12472 Ayah**, canonical active
  version `01a06fb5-19b2-7465-af74-4b579e50c7be`. Readiness `ok`, backend/web/gateway/
  PostgreSQL healthy, backup/heartbeat timers active. Свободно около 867 MiB;
  никакой очистки или снижения backup threshold не выполнялось.
- Android: при начале этой проверки пользователь уже выбрал **Мадинский Мусхаф,
  страницу 51 / 3:11** (не прежнюю QCF 2/2:1). Аудио Saad al-Ghamdi / Аль Имран · 16
  было на паузе. Именно это текущее место следует сохранить после QA.
  Через штатную кнопку обновления **KFGQPC полностью скачан и активирован:
  604/604, 196 MiB** (раньше 217 MiB). Новая страница 51 отобразилась с новым
  шрифтовым растром, фиксированной сеткой строк и правильным выделением 3:11.
  Доказательство: `android-kfg-v2-page51-loaded.png` в том же tmp-каталоге.
  QCF показал подтверждение нового пакета **197 MiB** вместо старого 248 MiB;
  загрузка запущена, но её завершение на телефоне ещё не подтверждено.
- **Device QA остановлена безопасно:** снимки для short tap/rotation оказались
  чёрными из-за сна устройства (`mWakefulness=Asleep`, причина `timeout`), поэтому
  не считать их успешной проверкой. После пробуждения Android показал отсутствие
  питания, 28% батареи и **51 °C**. Причина нагрева не установлена. Приложение
  `forum.iqro.app` остановлено через `am force-stop`, без очистки данных/загрузок,
  дальнейшая нагрузка прекращена. Автоповорот **1/0** восстановлен и проверен;
  Wi-Fi/сотовая сеть не менялись. Оставлено выбранным KFGQPC, страница **51 / 3:11**;
  аудио не запускалось. После остывания и восстановления питания: проверить QCF
  604/604 (при необходимости продолжить загрузку), оба новых издания offline,
  short tap/hold/landscape → portrait, затем вернуть выбранный Мадинский Мусхаф.
  Старая device QA и source/Flutter tests не заменяют этот незавершённый повтор.

### 2026-09-08 — предыдущий checkpoint: типографика готова; APK установлен; SSH блокировал доставку

- Пользователь обнаружил неодинаковые межсловные и межстрочные промежутки.
  Предыдущие corpus/offline/audio проверки подтверждают целостность и работоспособность,
  **но не являются одобрением типографики для production**.
- В Flutter найдены две искусственные коррекции: распределение свободной высоты
  между проверенными разрезами строк и обрезка боков на 2.5%, зависящая от границ
  hit-map. Первая зависела от количества найденных границ, вторая могла менять
  масштаб между страницами/изданиями. Обе убраны из `MushafScanLayout.page`.
  Страница теперь отображается целиком одним равномерным преобразованием;
  выделение и hit-testing используют то же преобразование. Landscape сохраняет
  заполнение ширины и вертикальную прокрутку; прежний bleed скана Madani не изменён.
- Убрана ненужная пиксельная проверка межстрочных разрезов при открытии страницы.
  Проверка безопасных границ фрагмента аята для подробностей сохранена.
- Первоначальный запуск тестов остановился из-за **errno 28**. После отдельного
  разрешения пользователя удалён только `/Users/user/.gradle/caches/8.14/transforms`;
  это разрешение исчерпано, дальнейшая очистка не разрешена. Полный повтор Flutter:
  **361 passed, 13 opt-in skipped**; `flutter analyze --no-pub` без замечаний.
  Первый profile APK arm64/staging собран и установлен через `adb install -r` на Redmi
  Note 7 (`91aedea7`), без сброса данных. APK SHA256:
  `c5110f5ce353389509f51645559675834345140e0ffa76c62e3e1ad52b7aeea9`.
  Сохранено пользовательское чтение **QCF, страница 2 / 2:1**.
- Другая причина была записана в WebP: `prepare.build_page` и `kfgqpc.compose`
  использовали переменную высоту строк по ink bounds и распределяли свободную
  ширину между словами. Старое измерение на canvas 1000×1600:
  QCF страница 255 — высота строк 82.4–116.1, межсловные зазоры 7.0–25.0;
  KFGQPC страница 255 — 86.2–110.1 и 12.8–48.2 соответственно.
  Это реконструированная раскладка IQRO, не факсимиле исходного издания.
- Оба генератора теперь используют общую `PageTypography`: постоянный шаг строк,
  единый размер текста на странице, общие ascent/descent и ограниченные пробелы.
  Исходные слова, строки, огласовки, пропорции контуров и ссылки аятов сохранены.
  Короткая строка не растягивается пробелами до левого края. Семантическое
  центрирование QCF берётся из базы; QF 5 — по начальным страницам и исходному
  конечному номеру аята суры. Подробности: `ops/mushaf-glyph-pilot/TYPOGRAPHY.md`.
- На закреплённых источниках **35 Python tests passed без skips**, включая
  контрольные страницы 1/2/3/50/77/255/604, cmap p245 и новый typography validator;
  **4 Node raster tests passed**. Полный source audit 604×2 прошёл: 34 tests,
  9 отдельных pilot/source tests skipped (они проверены отдельным запуском выше).
  После финального уточнения центрирования окончания суры повторный полный
  KFGQPC typography audit тоже прошёл (2 tests, 604 страницы, без skips).
  Полный QCF audit относится к неизменённому renderer.
  Собраны **новые immutable версии** `qcf-v2-iqro-20260908-v4` и
  `kfgqpc-iqro-20260908-v2`. Старые версии и offline-загрузки не удаляются.
- Штатный staging backup безопасно отказался при 886 MiB (<1024 MiB).
  По отдельному разрешению пользователя свежий custom dump передан через SSH
  в закрытый каталог Mac; `pg_restore --file=/dev/null` прочитал все блоки.
  Дамп `quran_staging_20260907T220911Z.dump` (135624691 байт), SHA256
  `777959bea9e22d030c4ca169bc2f6aee419cdbbcea76e74c2574dbe02b8ba597` загружен
  в существующий приватный bucket через RAM-контейнер с `--keep-existing`;
  полное обратное скачивание и SHA verification прошли. Порог защиты не снижен,
  файлы на сервере не очищались, secrets и runtime-конфигурация не менялись.
- В существующей карточке offline-Мусхафа отсутствовала возможность обновления:
  при `ready` кнопка скрывалась, а установленная карта страниц хранится длительно.
  Добавлена **«Проверить обновления»** (RU/EN/AR/TR), только по нажатию, без фоновых
  проверок. Совпадающий package ID не перекачивается; новый требует подтверждения
  размера. Старый пакет остаётся активным до атомарной активации полного нового.
  Смена выбранного издания во время запроса отменяет устаревший UI-сценарий.
  **3 widget tests passed**: актуальная версия, новая с отменой/подтверждением,
  ошибка сети с сохранением доступности. Полный Flutter повтор: **364 passed,
  13 opt-in skipped**, анализатор без замечаний; финальный profile APK собран.
  Финальный APK установлен поверх прежнего без удаления данных; SHA256
  `831d6ae6998f359a6dcea14d4fee03685fd84e3a6359f890ed2131c9ecc7c713`.
  Первый установленный APK выше не содержал этой кнопки и заменён финальным.
- Проверки Android геометрии: QCF страница 3, короткий тап выделяет **2:7**,
  длинный открывает фрагмент этой же страницы, перевод/тафсир доступны.
  Landscape → portrait сохраняет видимость текста и выделение. Исходные системные
  rotation 1/0 восстановлены; пользовательское чтение возвращено на **2 / 2:1**.
  Новые изображения ещё не участвовали в device QA — их публикация блокирована.
  Финальная кнопка обновления видна на устройстве (в выборе Мусхафа ниже списка,
  карточка прокручивается). Исходный автоповорот 1/0 возвращён, сеть не менялась.
- Оба полных bundles прошли независимые проверки: по одному backend integration
  test с 604 страницами/6236 canonical ayahs/1812 mocked uploads и по одному Flutter
  test с реальным декодированием **1812/1812 WebP каждого издания**, SHA, размерами
  и hit-testing. Backend использовал только изолированную test database.
  Local release receipts: `verification.json` внутри каждого полного bundle.
  QCF tail 401–604 рассчитан отдельно тем же immutable renderer identity; после
  проверки SHA всех файлов подключён атомарными hard links, receipt последним;
  основной builder повторно проверил все 604 receipts и собрал единый manifest.
- Код, тесты и первая запись результатов закоммичены: **`6e346a7`**.
  Полные данные сохранены в workspace (не в очищаемом `/private/tmp`):
  `tmp/mushaf-typography-20260908/qcf-v4-final` и `kfgqpc-v2-final`.
  Manifest SHA256 соответственно:
  `fab5f2004c4ce11415d5eb393b8b24520f722b19f754ccde30a199b07d4a41be`,
  `3e7bf01a9b2ed70c3b2664dce5933dd2b5b52cf2a09b94be30fd6c55e733ab22`.
- **Блокер:** попытка передачи KFGQPC остановилась до SSH handshake:
  `162.55.35.8:22 Operation timed out`. Ещё две проверки дали тот же результат.
  HTTPS readiness при этом `ok` (database/cache/throttling true). Public catalog
  подтвердил прежние `kfgqpc-iqro-20260907-v1` и `qcf-v2-iqro-20260907-v3`.
  Передача/remote command не начались, никаких новых release pointers нет.
  Текущий исходящий IPv4 Mac: **46.96.39.55**; по `docs/staging.md` SSH ограничен
  текущим IP. Несовпадение firewall allowlist — предположение, не подтверждённый
  диагноз. Firewall/VPN/SSH policy без отдельного согласования не менять.
- Следом после восстановления доступа: проверить актуальность backup и runtime;
  опубликовать bundles **последовательно** через существующий RAM override,
  `publish_mushaf_rendition --validate-only`, затем `--upload-workers 4`;
  проверить API/CDN/SHA и неизменность fingerprint чтецов/аудио/canonical;
  на Android через «Проверить обновления» скачать новые версии, проверить
  offline/tap/hold/landscape и вернуть **QCF 2 / 2:1**, аудио на паузе.
  Готовые локальные помощники: `publish_bundle_staging.py`, `verify_live.py`,
  `audit_staging.py` в том же `tmp/mushaf-typography-20260908` (читать перед запуском).
  **Новые страницы ещё не опубликованы и не скачаны телефоном**. Backend runtime
  остаётся `5b4ae94`: код/миграции для этой корректировки не менялись.
  Не объявлять этап доставки или production release завершённым.

## Состояние 2026-09-07

### Актуальный checkpoint: KFGQPC опубликован, Android/offline QA пройдено

- Пользователь подтвердил offsite-передачу конкретного дампа
  `quran_staging_20260907T200827Z.dump` в прежний приватный bucket. Upload с
  `--keep-existing` и полное обратное скачивание/SHA verification прошли;
  старые резервные копии сохранены.
- На staging доставлен commit `a0624fb`, применена добавочная миграция `0006`.
  Backend собран штатным Dockerfile; неизменённые web/gateway/postgres/caddy
  образы переиспользованы без повторной сборки. Секреты и volumes не менялись.
- `kfgqpc-hafs@kfgqpc-iqro-20260907-v1` включён атомарно после **1812/1812**
  immutable uploads/checks. Передача полного набора на сервер выполнена через
  одноразовый RAM mount (512 MiB; контейнер ограничен 1 GiB, наблюдалось около
  541 MiB), без второй дисковой копии bundle. Локальный набор сохранён.
- Проверка публичного API выявила PostgreSQL-specific ошибку `jsonb = varchar`
  в source checksum subquery. Исправлена `KeyTextTransform`, добавлена регрессия
  с настоящим PostgreSQL SQL compiler: fail до исправления, pass после.
  **762 backend tests passed, 7 opt-in skipped**, coverage 82.68%; Ruff/format
  и Mypy (270 файлов) зелёные. Дополнительный read-only прогон на реальном
  staging PostgreSQL проверил catalog/page/offline для обоих изданий и закрытый
  production gate. Никакие тестовые таблицы в рабочей базе не создавались.
- Действующий backend/worker/beat commit **`5b4ae94`**. Исправленный immutable
  образ собран поверх точного `a0624fb` image с заменой единственного изменённого
  runtime-модуля; dependency lock/Dockerfile/остальной runtime не менялись.
  SHA модуля в работающем контейнере совпал с Git. Повторная полная сборка при
  ограниченном диске не выполнялась. Не использовать промежуточный `a0624fb`
  как rollback target: в нём есть известная ошибка SQL.
- Public API с мобильными `Accept`/`Accept-Language` показывает оба native
  издания. Все три ширины страниц **1, 3, 50, 77, 604** скачаны с CDN и проверены
  по SHA/размеру. KFGQPC offline: 604 страницы, 2160 px, **228 046 646 байт**,
  checksum `da6f1e6081148a1570f7dfd04603e18fd92c69cd56b06bb9084e14088e02b094`.
- После публикации все прежние fingerprints снова совпали: 18 чтецов с admin
  портретами, 36 recitations, 4104 tracks, canonical edition/12472 ayahs двух
  версий. Обычный Мадинский Мусхаф и ранее скачанный QCF не изменены.
- На установленной profile APK Redmi Note 7 выбран KFGQPC, открыта страница 50.
  Short tap выделил **3:7**, play запустил «Аль Имран · 7» / Saad al-Ghamdi
  (MediaSession state=3, error=null). Hold открыл фрагмент той же страницы с
  оригинальным шрифтом и переводом. Landscape → portrait: текст виден, страница
  и выделение сохраняются; системные rotation 1/0 возвращены. Аудио на паузе.
- Перетягивание точек перевело KFGQPC **50 → 51**, `dumpsys vibrator` подтвердил
  OneShot 22 ms от `forum.iqro.app`. Чтение работало во время загрузки; разовый
  `dumpsys meminfo` показал ~293 MiB total PSS (не полноценный performance benchmark).
- Полная offline-загрузка завершена: **604/604, 217 MB/217 MB, ready**.
  После force-stop и cold start (3002 ms) при **Active default network: none**
  выбранный KFGQPC и offline-статус сохранились. Впервые открыты его страницы
  **604 → 603** из локального пакета, текст полностью виден. В просмотренном
  логе нового процесса `11756` нет Unhandled/SQLiteException/DatabaseException/
  FATAL/overflow/ANR. Wi-Fi/data восстановлены в 1/1, rotation в 1/0.
  Возвращено пользовательское место **страница 50 / 3:7**, выбран KFGQPC,
  аудио не воспроизводится. Завершение device QA — ночь 2026-09-08, без повторной
  установки APK. Полное скачивание заняло около 10 минут на этой сети/устройстве;
  это фоновая установка, чтение при ней оставалось доступным.
- Доказательства: `/private/tmp/iqro-kfgqpc-download-ready-check-20260907.png`,
  `/private/tmp/iqro-kfgqpc-ayah7-selected-20260907.png`,
  `/private/tmp/iqro-kfgqpc-ayah7-playing-20260907.png`,
  `/private/tmp/iqro-kfgqpc-ayah7-details-20260907.png`,
  `/private/tmp/iqro-kfgqpc-landscape-20260907.png`,
  `/private/tmp/iqro-kfgqpc-portrait-return-20260907.png`,
  `/private/tmp/iqro-kfgqpc-page604-offline-20260908.png`,
  `/private/tmp/iqro-kfgqpc-page603-offline-20260908.png`.
- Health/runtime budget зелёные, backup/heartbeat timers активны. Web smoke
  RU/EN/AR/TR выполнен в браузере. На сервере осталось около **0.85 GiB**:
  дальнейшие тяжёлые сборки требуют отдельного решения по ёмкости; **никакой
  очистки диска не выполнялось**. Production/store release не выполнялся.

### История: подготовка существующего KFGQPC HAFS (Quran.Foundation 5)

- Подготовлен отдельный адаптер **существующих** слов/строк QF 5 и официального
  Unicode-шрифта `UthmanicHafs1Ver18`. Это не ещё один JMApps QCF. Полный read-only
  snapshot закреплён по SHA; все 604 страницы и 6236 аятов прошли геометрический
  аудит. Изображения готовятся отдельно от API/телефона в 720/1440/2160 px.
- Публикация принимает `kfgqpc-hafs` в общий `/mushaf-renditions`, сохраняя
  source metadata вместо выдуманного Git commit. Добавочная миграция `0006`;
  canonical/audio/портреты не импортируются и не меняются. Смена QF source SHA
  скрывает старое производное оформление без удаления данных.
- Исправлен **production blocker Flutter**: раньше любая дополнительная визуальная
  версия принудительно заменялась старым сканом. Теперь production допускает
  только опубликованные `staging_only=false` записи из своего каталога; preview
  или одна сохранённая preference доступа не дают. Каталог сохраняется для offline.
  RU/EN/AR/TR различают опубликованное издание и staging preview.
- Код: **761 backend tests passed, 7 opt-in skipped**, coverage 82.68%; Ruff/Mypy
  зелёные, **354 Flutter tests passed, 13 opt-in skipped**, analyze без замечаний;
  **26 source/renderer Python tests** прошли, включая прежние QCF-шрифты.
- На этом checkpoint полный растровый bundle ещё строится; **не считать KFGQPC
  установленным на сервере/телефоне или одобренным для production**. Следом:
  full-bundle проверки, точечный staging deploy после backup, publication и device QA.
  Другие QF варианты (включая V4 Tajweed) этим адаптером автоматически не подключаются.
- Воспроизводимая подготовка: [KFGQPC.md](../../../ops/mushaf-glyph-pilot/KFGQPC.md).

#### Завершение локальной подготовки KFGQPC

- Код подготовки/публикации/production-каталога: `894dfa2`. Полный bundle
  `/private/tmp/iqro-kfgqpc-full-20260907` завершён: **604 страницы, 1812 WebP**.
  Manifest SHA `cc9419d4cd157e1ced820578048b5ebe3a5e0749212eb81bc803c963af91a0e6`.
  Raster bytes: 720 → 62 919 514; 1440 → 144 454 582; 2160 → 228 046 646.
  Сумма всех разрешений: 435 420 742 байта. Исходный WOFF2 — 87 760 байт.
- Сравнение всех 604 page/ayah sets с canonical staging snapshot: **0 различий**.
  Отдельный backend full-bundle тест прошёл (1812 mocked uploads, real integrity
  validation, unchanged canonical UUID); Flutter full-bundle тест прошёл:
  реальное декодирование всех 1812 файлов и hit-testing всех аятов.
- Контрольные страницы 3, 77, 604 просмотрены. IQRO-раскладка сохраняет исходные
  слова/строки и оригинальный шрифт; короткие строки центрируются без растяжения
  текста. Это не полный редакционный просмотр 604 страниц и не facsimile sign-off.
- Дополнительно исправлен cold-start production: сохранённый опубликованный каталог
  читается сразу из SQLite, без ожидания HTTP timeout. Свежий пустой каталог имеет
  приоритет и отзывает доступ; staging/prod caches разделены по API URL.
  **356 Flutter tests passed, 13 opt-in skipped**, analyze зелёный; full-bundle
  проверка выполнена отдельно. Backend итог: **761 passed, 7 skipped**, Mypy/Ruff
  зелёные; 26 Python source tests и 4 Node raster tests прошли.
- На сервере создана локальная backup-копия
  `quran_staging_20260907T200827Z.dump`, pg_restore structure verified,
  `BACKUP_RETENTION_DAYS=0`. Старые копии сохранены. **Автопроверка разрешений
  отклонила offsite upload**; пользователь запрошен о передаче конкретного дампа
  в прежний приватный `iqro-postgres-backups`. До подтверждения/проверки offsite
  код и KFGQPC на сервер не передаются; миграция `0006` на staging не применялась.
  Действующий backend SHA остаётся `5ca2efa`.
- Перед deploy все прежние fingerprints совпали: 18 чтецов, 36 recitations,
  4104 tracks, 1 canonical edition, 12472 ayahs двух версий. Никакой очистки
  диска в этом этапе не выполнялось. Production/store publication не выполнялась.
- Cold-start исправление и результаты полного набора закоммичены в `4a5969c`.
  Финальная staging profile APK собрана (36.3 s), 58 376 794 байта, SHA-256
  `5b8b0712cec6a5a66eb743b1053198f95f83b5d303008405fafa145b024eb7ef`.
  Установлена поверх `forum.iqro.app` на Redmi Note 7 / `91aedea7`: `Success`.
  Это обновление **кода**, не установка KFGQPC-контента на устройство: его каталог
  появится после разрешённого staging deploy/publication. Полное device QA нового
  KFGQPC (audio/hold/rotation/offline) остаётся после публикации.

### Предыдущий checkpoint: QCF V2 опубликован на staging

- Staging восстановлен без повторной сборки после согласованного удаления только
  42 новых BuildKit cache IDs. Действующий backend commit `5ca2efa`, health/runtime
  checks и timers зелёные; прежние backups, volumes и секреты сохранены.
- `qcf-v2-hafs@qcf-v2-iqro-20260907-v3` атомарно подключён после **1812/1812**
  uploads и проверок. Public API: 604 страницы, widths 720/1440/2160.
  Публичные файлы страниц 1, 3, 245, 604 сверены по размеру/SHA во всех разрешениях.
  Default offline manifest: 604 страницы, 259 748 712 байт, checksum
  `5f3801c48e621d5ae849c6050b2832700bffafa9a15677d11cb9884098d1e9ca`.
- После publication все 5 fingerprints каталогов ниже снова совпали: чтецы,
  загруженные admin портреты, 36 recitations, 4104 tracks и canonical IDs сохранены.
- Новый вариант появился на Android после штатного обновления edge cache (ключ
  зависит также от `Accept`; проверять с `Accept: application/json`, `Accept-Language: ru`).
  Кэш не очищался. SHA установленной APK совпал с подготовленной `737c5644…`.
- На Redmi Note 7 открыты QCF страницы 126 и 127; короткий тап выделяет 5:111,
  play запускает «Аль-Маида · 111» / Saad al-Ghamdi, MediaSession state=3/error=null.
  Удержание: фрагмент той же QCF-страницы, перевод и тафсир. Портрет → альбомный →
  портрет: страница сохраняется, текст виден; исходные rotation 1/0 восстановлены.
- Device QA обнаружил **реальный offline blocker**: SQLite на Redmi отвергает
  `ON CONFLICT ... DO UPDATE` (`near "ON": syntax error`). Тот же код был в аудио.
  Подготовка items заменена общим transactional batch: INSERT OR IGNORE + UPDATE,
  без REPLACE/удаления записей, с сохранением прогресса при прежних SHA/размере.
  7 новых регрессий + профильный прогон **17 passed**, анализатор без замечаний.
  Полный обычный прогон: **349 passed, 13 skipped** (opt-in source/full-bundle
  проверки выполнялись отдельно до deploy, см. ниже). Исправление: `17810ae`.
  Profile staging APK собрана и установлена поверх приложения (`Success`):
  58 376 794 байта, SHA-256
  `15a794e841d6242b714089c69c1f5ff6ef5399d44d3c1ce002778e86a99f14b7`.
  Повторная offline-загрузка завершена: **604/604, 248 MiB/248 MiB**, ready.
  Переключение на старый Мусхаф и обратно не прерывает загрузку. Дополнительно
  открыта QCF-страница 245. После force-stop/restart с Wi-Fi/data выключенными
  (`Active default network: none`) впервые открыты страницы **604 → 603** из
  локального пакета. Wi-Fi/data возвращены в исходное 1/1, rotation в 1/0.
  Старый Мусхаф, портреты, скачанные страницы и настройки сохранены.
- Проверка restart-лога выявила второй Android-specific дефект: `SELECT *` статуса
  захватывал весь QCF manifest и превышал CursorWindow (`Row too big`). Чтение
  страниц работало, индикатор offline после перезапуска — нет. Status/list/delete
  queries ограничены нужными колонками; старые audio manifests читаются небольшими
  chunks только для quality/reciter. Никакие manifests не удаляются/переписываются;
  не нужна миграция или повторная загрузка. Добавлены 4 регрессии, включая >2 MiB
  Unicode manifest и список больших Mushaf пакетов. Профильный прогон: **21 passed**,
  анализатор зелёный. Повторный полный прогон: **353 passed, 13 opt-in skipped**.
  Исправление `8d3a19c` зафиксировано. Финальная profile APK установлена (`Success`),
  SHA-256 `34000a9adeb49c45cb358a4059d55d4c465b99c755e099fa134f27f9bc624962`.
  После нового запуска **без сети**: каталог показывает выбранный QCF и
  **«Доступен без интернета · 604/604 · 248 MB/248 MB»**, без повторной загрузки.
  Страница 126 открыта из сохранённого пакета. В просмотренном логе финального
  процесса `28520` нет SQLite/Flutter Unhandled/FATAL/overflow/ANR; это не soak
  benchmark. Wi-Fi/data возвращены в 1/1; пользовательское место 126 / 5:109
  возвращено после тестовых переходов. Приложение оставлено на QCF, аудио на паузе.
  Backend-образ не требует повторной сборки.
- Техническое подключение полного QCF V2 на staging и Android завершено.
  Доказательства устройства: `/private/tmp/iqro-qcf-final-ready-after-restart-20260907.png`,
  `/private/tmp/iqro-qcf-final-offline-page126-20260907.png`,
  `/private/tmp/iqro-qcf-page604-offline-20260907.png`,
  `/private/tmp/iqro-qcf-page603-offline-20260907.png`.
  Backend SHA остаётся `5ca2efa`; mobile-only исправления не требуют Docker build.
- Это staging preview оформления IQRO, не официальное факсимиле и не разрешение
  production/store release. Контентный review и остальные release gates остаются.

### История выполненных этапов и предыдущих ограничений

- Этап 1: `1db18d2`, Android native 22 ms page tick, настройка приложения,
  системный opt-out, unit/widget-тесты. Profile APK установлен; `dumpsys vibrator`
  показывает OneShot 22 ms от `forum.iqro.app` при перелистывании. Пользователь
  подтвердил: первый пункт проверен на телефоне и работает. Этап закрыт.
- Этап 2, предыдущий checkpoint: выполнен [аудит источников](MUSHAF_SOURCE_INVENTORY.md), не установка
  новых изданий. После подтверждения этапа 1 начат
  [QCF V2 native pilot](../../../ops/mushaf-glyph-pilot/README.md): закреплённые
  входные файлы, аудит полной glyph DB, контуры шести страниц, 18 lossless WebP
  и точная карта аятов. 16 Python + 4 Node теста, 12 source-проверок во Flutter;
  общий Flutter-прогон 345 тестов зелёный. Размер/масштаб строк уточнены после
  визуального просмотра, source-тесты повторены. Вариант **не установлен** в APK
  и не опубликован на staging; выбор, API/cache/offline ещё нужно реализовать.
- Этап 3: `f1b6be0`, offline календарь Умм аль-Кура с поправкой, сеткой и
  источниками; входит в установленную финальную APK `c31533f`. На Redmi Note 7
  проверены открытие с главной, календарная сетка, текущая дата и отметки дней.
- Этапы 4–5: главная с календарём/закатом по текущему расписанию и мотивом арки;
  один статичный polished frame во всех видах портрета, admin URLs неизменны;
  1.1 s однократное появление существующего launcher logo, reduced motion.
  На устройстве найдены и исправлены сужение карточки чтения и разделитель тысяч
  в годе Хиджры. `c31533f` добавляет проверки ширины на 4 локалях/крупном тексте.
  Финальная главная проверена на Redmi Note 7; аватарки администратора сохранены.
- Этап 6: прежняя блокировка `No space left on device` снята после явного согласия
  пользователя удалить **только** `clients/iqro_mobile/build` (около 1.2 ГБ).
  Исходники, `.dart_tool`, секреты и данные телефона не удалялись. Сборочный каталог
  затем создан заново штатной компиляцией.
  Полный повторный прогон: **333 теста прошли**, `flutter analyze --no-pub` без
  замечаний; lockfile, форматирование, генерация локализаций проверены.
  Profile staging APK `c31533f` установлена поверх приложения на Redmi Note 7
  через `adb install --no-streaming -r`; размер 58 376 794 байта, SHA-256
  `e4a718ebb6cd2253daaa2a9573b479c3742d43caad583e2238cac7560816469b`.
  Это **не** production signing/store upload и не завершённый release gate.

### Проверка Android после повторной сборки

- Страница Мусхафа 91 загружается, жест переводит на 92; короткий тап выделяет
  аят и показывает панели; перетягивание по соседним точкам возвращает на 91.
  В `dumpsys vibrator` новые события от `forum.iqro.app`: OneShot 22 ms, usage 13,
  в 10:03:17 (жест страницы) и 10:12:12 (точки), время телефона 2026-09-07.
- После этого прогона пользователь подтвердил работоспособность первого пункта,
  включая проверку на телефоне. Исторические ограничения ниже относятся к ADB-прогону.
- В предыдущей установленной APK этого же этапа (`38655fa`, до двух layout-правок)
  проверены загрузка портретов и запуск аудио касанием чтеца: MediaSession active,
  state=3, speed=1, position=25247, error=null; Аль-Бакара / Abdul Basit Abdus Samad.
  Перед финальной установкой воспроизведение поставлено на паузу.
- На финальной APK видна каноническая фотография того же чтеца в мини-плеере.
  Завершающая попытка повторно открыть полный плеер не выполнена: телефон отключился
  от USB. Не выдавать её за проверенную на устройстве. Widget-тесты плеера зелёные.
- В просмотренном логе финального процесса нет Flutter fatal/overflow/ANR;
  есть системные MIUI `Perf.getFolderSize` NPE при запуске. Это наблюдение лога,
  не полноценный performance/soak benchmark.

Локальные диагностические файлы (не архив релиза):
`/tmp/iqro-mobile-ui-correction-tests-20260907.log`,
`/tmp/iqro-mobile-final-profile-build-20260907.log`,
`/tmp/iqro-home-final-20260907.png`, `/tmp/iqro-mushaf-dot-drag-result.png`.

### Подключение полного QCF V2 — текущая работа

- Закреплены и скачаны все 604 постраничных шрифта того же источника; исходные
  файлы не включаются в APK. Renderer v3 сохраняет контуры и исправляет в памяти
  повреждённую служебную cmap-таблицу строго известного файла страницы 245.
- Read-only сверка со staging: полное совпадение всех **6236** связей аят/страница
  с действующей canonical version `01a06fb5-19b2-7465-af74-4b579e50c7be`.
- Реализованы отдельные модели/атомарная публикация/backend API, русская read-only
  админка; Flutter selection/cache/offline/excerpts. Preview закрыто в production.
  По одному download controller на издание; переключение не дублирует загрузку.
- Backend: **752 passed, 6 skipped**, покрытие 82.92%, Ruff/Mypy без ошибок,
  `makemigrations --check` без расхождений. Дополнительный opt-in тест полного
  bundle на изолированной БД прошёл: validate-only без записи, 1812 uploads через
  тестовый адаптер, 604 записи, все 6236 canonical IDs сохранены.
- Финальный Flutter: **354 passed**, включая 12 source pilot tests; отдельно
  прошёл full-bundle test с декодированием **всех 1812 WebP**, SHA и попаданием
  в аяты на всех 604 страницах. `flutter analyze --no-pub` без замечаний.
  Python: **20 passed** с обоими source-наборами, Node: **4 passed**.
- Полный bundle: `/tmp/iqro-mushaf-full-render-20260907-verified`, версия
  `qcf-v2-iqro-20260907-v3`, manifest SHA-256
  `d7ed73f07958e6de1927a1a4d56d0f912668a5010b361bf1d982f037e70d2130`.
  Изображения 720 px: 71 922 630 байт; 1440 px: 164 631 364 байта;
  2160 px (высокое качество/offline): 259 748 712 байт. Это отдельные загрузки,
  а не дополнительные 496 МБ внутри APK. Source lock находится в Git.
- **Ещё не staging deployment:** до обновления staging требуется проверенная
  резервная копия. Автоматическая проверка безопасности запросила явное разрешение
  на отправку БД в настроенный приватный R2 bucket `iqro-postgres-backups`, prefix
  `quran-platform/postgres/staging`. Старые копии не удалять; данные/секреты не выводить.

### Преддеплойная проверка после разрешения backup

- Пользователь разрешил отправку staging-БД, включая аккаунты, в указанный private
  R2 bucket/prefix без удаления прежних копий. Повторное разрешение на этот же
  backup не требуется.
- В `offsite.py` добавлен явный `upload --keep-existing`; тест подтверждает, что
  режим не перечисляет и не удаляет прежние объекты. Все 7 backup-тестов зелёные.
  Для локальной копии нужен runtime `BACKUP_RETENTION_DAYS=0`, без правок secret env.
- Новый blocker: на staging `/dev/sda1` 38 ГБ, занято 35 ГБ, доступно **924 МБ**
  (ниже штатного minimum free 1024 MiB). Backup/deployment/publication не запускались;
  действующий commit остаётся `b637fa350d83c49985c55061cf3f6345dd9f467f`.
  Требуется согласованное освобождение места или увеличение диска. Ничего не удалено.
- Redmi Note 7 `91aedea7` снова определяется по USB; новый QCF нельзя проверить
  end-to-end до завершения staging publication. Установленная APK остаётся прежней.

### Попытка staging deployment 5ca2efa: требуется дополнительное место

- Пользователь согласовал точные 51 ID старого BuildKit-кэша. Адресная обработка
  выполнена в два прохода (дочерние/родительские зависимости); без расширения списка,
  без удаления images/volumes/backups. Перед сборкой свободно 3.8 GiB.
- Backup `quran_staging_20260907T164810Z.dump` создан с local retention=0,
  отправлен с `upload --keep-existing`; локальная SHA/структура и полное offsite
  скачивание/проверка успешно завершились. Предыдущие копии сохранены.
- На сервер через git archive передан `5ca2efac9cc09bd5721563ff9ab422d18a12132e`.
  Все 5 staging images собраны. Но новый build-кэш занял остаток диска:
  `/dev/sda1` available=0. При `up --no-build` PostgreSQL не стартовал:
  `could not write lock file postmaster.pid: No space left on device`.
  Остальные новые сервисы остались Created. **Staging временно недоступен.**
  Миграция QCF и публикация ещё не выполнены. Нельзя считать deployment успешным.
- Новый кэш не входит в разрешённые 51 ID; его не удалять без нового согласия.
  Точный новый список: `/private/tmp/iqro-staging-new-build-cache-20260907.md`.
  После согласованного освобождения места **не повторять сборку**: выполнить
  compose с теми же production/staging/budget файлами, `APP_VERSION=staging-5ca2efa`,
  `up --no-build --detach --wait`, затем `make staging-budget-runtime-verify` и ps.
  После recovery передать QCF bundle и выполнить validate-only/publication/Android QA.
- До обновления read-only fingerprint: Reciter 18, RecitationEdition 36,
  AudioTrack 4104, QuranEdition 1, Ayah 12472 (две canonical versions).
  SHA-256: Reciter `48d3418bd4c11bad48345f633ee4780fe3f325acfd0c7914fc6d03664070797e`,
  editions `348e2b266238c21e3dfb2f70901025379c541aa0c1dbf0896b7383a4af42f32f`,
  tracks `e21262390695eed29225efa4a529402a7223b0105dea4486d49ee87055beb0bc`,
  QuranEdition `0f5c4c21526079e8c19ae8e343dc8398438853cf263823064d85f16976ad59a5`,
  Ayah `32bde1be772dff8aee118cd14f10112fe73994289da36e234b9eb12dd2ec7bfa`.
  Script повторной проверки: `/private/tmp/iqro-catalog-fingerprint-20260907.py`.
- Android `91aedea7` подключён, IQRO запускается. Исходные настройки поворота:
  accelerometer_rotation=1, user_rotation=0; не изменялись. QCF end-to-end пока не проверен.

### Staging восстановлен после отдельного разрешения на новый кэш

- Пользователь отдельно согласовал 42 ID нового кэша (около 3.12 GB). Удалены
  только эти записи через точные ID, с проверкой Reclaimable/Shared; images,
  контейнеры данных, volumes, backups и secret env не удалялись.
- Запущены **уже собранные** `staging-5ca2efa` images через `up --no-build`.
  PostgreSQL healthy; `quran.0005_mushaf_renditions` applied; полный runtime budget
  check зелёный. `/api/v1/health/ready`: database/cache/throttling=true.
  `/` перенаправляет на `/ru`; robots.txt запрещает индексацию; оба timer active.
- После recovery все пять catalog fingerprints выше совпали байт-в-байт.
  В браузере открыты RU/EN/AR/TR; на RU загрузились каталоги чтецов и сур.
- Полный QCF bundle передан отдельно в
  `/opt/quran/mushaf-renditions/qcf-v2-iqro-20260907-v3` (483 MiB).
  Manifest SHA совпал; **серверный validate-only: 604 pages verified; no writes**.
  Свободно около 2.3 GiB. Начата immutable publication; включение каталога
  только после всех 1812 uploads/проверок. Не считать начало публикации её завершением.
- Для следующей сборки текущего запаса недостаточно: прежние 3.8 GiB были заняты
  пересозданным build-кэшем. До нового build требуется отдельное планирование места;
  не запускать очистку автоматически и не повторять build ради старта готовых images.

### Предыдущий Android checkpoint полного подключения

- Код `a6bd353` зафиксирован. Profile staging APK установлен через
  `adb -s 91aedea7 install --no-streaming -r` на Redmi Note 7; ответ `Success`.
  Размер 58 376 794 байта, SHA-256
  `737c56448af09b53beea20ffa01e043774391c571b506a783a73cb85be064e06`.
  Приложение не удалялось, локальная БД/скачанные страницы/настройки не очищались.
- После запуска: главная, сохранённый Саад аль-Гамди в мини-плеере; каталог
  чтецов и круглые портреты загружаются. Медиасессия IQRO наблюдалась active,
  state=3, speed=1, error=null, затем оставлена на паузе (state=2).
  Это не подтверждение слышимости, полного playthrough или всех таймкодов.
- Вкладка «Коран» и первая страница **действующего** Мадинского Мусхафа открыты.
  Выбор/офлайн-пакет QCF V2 на устройстве ещё не проверялись: серверная часть
  не деплоена, новая запись каталога ещё не опубликована.
- В просмотренном логе процесса нет Flutter fatal/Unhandled/overflow/ANR;
  есть MIUI Perf NPE и медленные кадры холодного старта. Не считать это
  performance benchmark или доказательством полного отсутствия задержек.
- Скриншоты: `/tmp/iqro-qcf-v2-installed-20260907.png`,
  `/tmp/iqro-qcf-v2-reciters-20260907.png`,
  `/tmp/iqro-qcf-v2-existing-mushaf-loaded-20260907.png`.
- Staging всё ещё на `b637fa350d83c49985c55061cf3f6345dd9f467f`, никаких записей
  в его БД/объектное хранилище в этом этапе не делалось. Перед следующим запуском
  backup учитывать, что старые backup scripts содержат retention/prune:
  разрешение на новую копию **не** разрешает удалять прежние копии.

Ближайшее продолжение: редакционная проверка новой раскладки и остальные release
gates; другие издания из source inventory подключать отдельно, без смешивания
данных. Corpus QA, backup, staging publication, выбор/тап/удержание/аудио/поворот,
полная offline-загрузка и restart на Redmi проверены в актуальном checkpoint выше.
Обе найденные Android SQLite/CursorWindow ошибки исправлены, финальная APK установлена.
Действующий Мусхаф, аудио и портреты сохраняются.
Для массового выпуска остаются
остальные проверки этапа 6, production signing и согласование публикации.
