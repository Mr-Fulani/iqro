# Таджвид: отдельная проба оригинальных страниц

## Что проверяем

По запросу пользователя: показать на Android книжную страницу без новой
верстки и без растягивания по одной оси. Это **визуальная проба трёх страниц**,
не замена QCF V4 и не публикация нового 604-страничного издания.

Проба доступна только при `TAJWEED_BOOK_PREVIEW=true`, не в release mode и не
при `APP_ENV=production`. В обычной сборке маршрут и пункт меню не включены.
Открытие: Мусхаф → меню «…» → «Таджвид · проба книжной страницы».
Прямой маршрут тестовой сборки: `/mushaf/tajweed-book-preview`.

## Точные исходники

Источник: [QuranHub/quran-pages-images](https://github.com/QuranHub/quran-pages-images/tree/c6f1455281e19d56a71a999498a323a828de3365/easyquran.com/hafs-tajweed),
commit `c6f1455281e19d56a71a999498a323a828de3365`, каталог
`easyquran.com/hafs-tajweed`. Репозиторий указывает GPL-3.0. Это не утверждение
о завершённой редакционной/лицензионной приёмке издания для публичного релиза.

Каждый файл — неизменённый исходный JPEG **645 × 1000 px**. Новые изображения,
глифы, пробелы, строки и цвета не создаются. Исходный JPEG не перекодируется.

| Страница | Байты | SHA256 |
| --- | --- | --- |
| 51 | 198649 | 71c7448348cd68a74c9cdf871439e6e044bd66092c110df42ea775a54836b323 |
| 52 | 200165 | e920cf216b94959443b4f0ba9e88223029418a733a6089c7c764642e259e01ab |
| 53 | 206161 | 8277c72b287a2f1310c3ba0b2cb64c63bf4024389c8fe11bcbddcb589b033db3 |

Общий размер 604975 bytes (около 591 KiB). Не скачиваем весь корпус.
Для визуальной пробы эти изображения загружаются прямо по закреплённым
GitHub URLs с проверкой размера/SHA; они не входят в APK. Это изолированный
test-only источник, **не production API-контракт**. Для настоящего издания
нужна версионная публикация через backend/CDN и штатный offline manifest.
Кэш находится отдельно в app support `tajweed-book-preview-v1`, адресуется
checksum и не затрагивает offline-пакеты других Мусхафов. Ничего не очищается.

## Отображение

- Один полный source rectangle, единый пропорциональный масштаб.
- В исходниках печать присутствует у всех четырёх краёв: уменьшать поля здесь
  нельзя, иначе будет потеря рамки или цветовой легенды. Crop равен полной странице.
- «По ширине» не добавляет боковые поля внутри безопасной области экрана;
  при нехватке высоты доступна вертикальная прокрутка всей страницы.
- «Вся страница» сохраняет всю страницу одновременно, с неизбежными полями
  при несовпадении пропорций экрана и источника.
- Pinch zoom; двойной тап сбрасывает масштаб. Одиночный тап скрывает/показывает
  меню. Поворот сбрасывает translation/zoom, сохраняя декодированный кадр.
- Собственные заголовки/номера/рамки поверх печатного оригинала не добавляются.
  Управление — скрываемый overlay, оно не раздвигает строки.
- Свайпы страниц физически RTL в RU/EN/AR/TR; один haptic на смену страницы,
  с сохранением пользовательского выключателя отклика.

## Чего здесь намеренно нет

Нет выделения аятов, аудио от тапа и сохранения места чтения: у EasyQuran другая
раскладка, готовой проверенной карты этого источника нет. Геометрию QCF V4
использовать запрещено. Основная читалка, выбор издания, аудио, аватары, аккаунт,
каноническая БД и текущие загрузки не изменяются. Staging не обновляется.

645 px недостаточно для качественного увеличения на современном экране.
Увеличение изображения не превращает его в HD. До полноценного подключения нужны
высококачественный источник той же книги, точная карта аятов и проверка всего корпуса.

## Воспроизведение проверок

```sh
cd clients/iqro_mobile
flutter analyze --no-pub
IQRO_TAJWEED_BOOK_SAMPLES=/absolute/path/to/three/original/jpegs \
  flutter test --no-pub --dart-define=TAJWEED_BOOK_PREVIEW=true \
  test/features/mushaf_facsimile_test.dart \
  test/integration/tajweed_book_sample_test.dart
flutter build apk --profile --no-pub --target-platform android-arm64 \
  --dart-define=APP_ENV=staging \
  --dart-define=API_BASE_URL=https://staging.iqro.forum \
  --dart-define=TAJWEED_BOOK_PREVIEW=true
```

## Проверки кода

- `flutter analyze --no-pub`: замечаний нет.
- Профильные layout/widget/source проверки: 11 passed, без пропусков.
- Полный mobile suite с включённой пробой и тремя исходными JPEG:
  **393 passed, 13 существующих opt-in tests skipped**. Пропуски относятся
  к другим исходным/full-bundle наборам, не к этим трём страницам.
- Поворот, вертикальная прокрутка, сохранение кадра, возврат в портрет,
  RU/EN/AR/TR, RTL-свайп, haptic callback, скрытие/показ меню, retry, source
  checksum и отсутствие обрезки печатных краёв проверены автоматически.

Фактический device-result добавляется после установки. Автотест не подтверждает
ощущаемую вибрацию, качество HD или полноценное подключение нового издания.
