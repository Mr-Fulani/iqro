# Локальный запуск команды

Общего тестового сервера нет. Backend работает на компьютере каждого разработчика;
браузер и мобильное приложение получают содержимое из его API.

## Инструменты

Для основного пути нужны Git, Python 3.9+, Docker Desktop с Compose v2 и `make`.
На macOS `git`/`make` устанавливаются с `xcode-select --install`; Docker Desktop
должен быть запущен. На Linux можно использовать Docker Engine с Compose plugin.
`make dev-doctor` проверяет подключение, миграции и готовность данных.

Host Python 3 используется только управляющим скриптом. Backend Python 3.14 и Node.js
устанавливаются Docker-образами. `uv` — менеджер Python-зависимостей; основной Docker-путь
обходится без его установки. Для работы с backend вне Docker установите `uv`, затем
в `services/backend` выполните `uv sync --frozen --all-groups`.
Для запуска backend вне Docker также нужны доступные PostgreSQL/Redis и переменные
окружения процесса: Django самостоятельно `.env` не загружает.

Mobile: Flutter 3.41.4, Android SDK либо полный Xcode 26+.
Для iOS также нужны Ruby 3.2+ и Bundler 4.0.3; CocoaPods и его зависимости
устанавливаются из `clients/iqro_mobile/Gemfile.lock`. Настройка Ruby:
[инструкция iOS](../clients/iqro_mobile/README.md#iphone-и-ipad). Выполните `flutter doctor`.
Для web вне Docker нужна версия Node.js из `services/web/package.json`, затем `npm ci`.

## Первый запуск

```bash
make dev-init
# В services/backend/.env указать QF_CLIENT_ID, QF_CLIENT_SECRET, QF_ENV
make up
# Либо сразу Android-приложение (backend подготовится автоматически):
make mobile-run
# iPhone/iPad: тот же backend, CocoaPods запускается через Bundler:
make mobile-ios-run
```

`QF_ENV=prelive` или `production` должен соответствовать выданным ключам **провайдера**.
Он не выбирает сервер приложения: приложение продолжает работать с локальным backend.
Секреты нужны только backend. После изменения `.env` повторите `make dev-up`, чтобы
контейнер получил новые значения. Существующий `.env` команда `dev-init` сохраняет.

`dev-up` собирает образы, запускает PostgreSQL/Redis, применяет миграции и запускает
API, автоматически подготавливает недостающие данные и только после успешной
проверки запускает web и фоновые задачи. Сайт при первом запуске получает готовую БД.
`make up` и `make` без аргументов выполняют `make dev-up`;
`make mobile-run` и `make mobile-ios-run` сначала выполняют тот же запуск.
После успешного `dev-up` команда автоматически пытается выполнить
`adb reverse tcp:8000 tcp:8000` для подключённого Android-устройства. Отсутствие ADB
или телефона не считается ошибкой и не мешает запуску web/backend.
При наличии старой БД и неприменённых миграций создаёт
дамп в `.dev/backups/` и проверяет его через `pg_restore --list`; при ошибке останавливается.
Проверка структуры архива не заменяет полноценную пробу восстановления production-бэкапа.

Сайт: http://localhost:3000, API: http://localhost:8000/api/v1,
документация API: http://localhost:8000/api/docs.

## Откуда берётся содержимое

| Данные | Подготовка | Хранение |
| --- | --- | --- |
| Канонический Коран | Закреплённый корпус Tanzil через публичное зеркало; SHA-256 проверяется | Локальная PostgreSQL |
| Слова и структура страниц | Content Sync Quran.Foundation | Локальная PostgreSQL |
| Соответствие аятов страницам | Проверенные 604 страницы QF KFGQPC, source 5 | `AyahPageMapping` |
| Web-Мусхаф | Все 13 макетов: локальные слова/разметка, официальные шрифты или изображения слов | БД + CDN, кеш браузера |
| Mobile-Мусхаф | Те же 13 макетов; Flutter загружает TTF или PNG слов и рисует страницу | БД + проверяемый SHA-256 кеш устройства |
| Ду’а | Полные встроенные каталоги из миграций; `dev-data` повторно проверяет/публикует их | Локальная PostgreSQL |
| Переводы и тафсиры | Автоматически все `QF_TRANSLATION_RESOURCE_IDS` и `QF_TAFSIR_RESOURCE_IDS` из `.env` | Локальная PostgreSQL |
| Аудио | Автоматически один чтец (159), все 114 сур и таймкоды; существующее полное аудио используется повторно | Метаданные в БД; поток с источника |
| Намаз | Методы из миграций и расчёт для выбранного места/даты | Локальный backend |

Миграции уже включают полный «Хисн аль-Муслим» и коранические ду’а из репозитория.
`make dev-init` копирует в `.env` полный список из 15 переводов и 13 тафсиров;
оставьте эти переменные явными, чтобы локальная конфигурация соответствовала набору,
который будет загружен. Если их нет в старом `.env`, backend временно использует
те же значения по умолчанию из настроек.
`make up` проверяет реальное состояние БД и выполняет только недостающие шаги:
полный каталог QF (13 макетов), импорт канонического текста в новое окружение,
переводы, тафсиры и аудио. Готовый канонический корпус сохраняется вместе с UUID.
Веб и Flutter получают данные через локальный backend; секретные ключи остаются в `.env`
backend. При первом открытии страницы клиент загружает официальные шрифты или изображения
слов из CDN. Это ресурсы источника, а не сканы или генерируемые локально страницы.

Макеты содержат 548, 604 или 610 страниц. Переходы по сурам/аятам используют индекс
выбранного макета. Прогресс и закладки сохраняют канонические ссылки, поэтому переключение
макета не меняет их смысл. Перечень и особенности: [все макеты QF](quran-foundation-mushafs.md).
Готовность проверяется для всех 13: одного загруженного KFGQPC недостаточно.
Если источник отдаёт неполные данные, импорт останавливается, сохраняя прежний каталог
и checkpoint. После восстановления сети повторите `make up`.

Первая подготовка требует сети и места для БД. Обязательной генерации 604 WebP больше нет.
Существующие публикации и файлы сохраняются. `--web-only` оставлен для совместимости
старых команд: web и mobile теперь используют один набор данных.
На компьютере с 8 ГБ RAM дождитесь окончания первого `make up` перед Android-сборкой
и эмулятором. Локальный Celery ограничен двумя процессами.

Периодические полные импорты Quran.Foundation отключены в local-настройках:
при запуске автоматически загружается недостающее. Для проверки обновлений макета QF,
переводов и тафсиров используйте `make dev-data DEV_DATA_ARGS=--refresh` или нужную команду импорта.
Фоновые задачи напоминаний и обслуживания локальной БД продолжают работать.

Первый запуск автоматически подготавливает потоковое аудио чтеца 159, если полного
аудио ещё нет. Для добавления другого чтеца используйте отдельную команду, например:

```bash
docker compose --env-file services/backend/.env exec backend python manage.py \
  sync_quran_foundation_audio --reciter-id 159 --all-surahs \
  --content-version local-dev-v1 --publish --resume
```

Это не скачивает все аудиофайлы в Git/media. Чтобы получить обновлённую неизменяемую
версию содержимого позже, задайте новое значение `--content-version`. Полный каталог
всех чтецов необязателен для обычной разработки и импортируется отдельно.

## Подключение телефона

| Устройство | API_BASE_URL |
| --- | --- |
| Android Emulator | `http://10.0.2.2:8000` (автоматически) |
| iOS Simulator | `http://127.0.0.1:8000` (автоматически) |
| Физический телефон в Wi-Fi | `http://<LAN-IP-компьютера>:8000` |
| Android по USB с `adb reverse tcp:8000 tcp:8000` | `http://127.0.0.1:8000` |

```bash
make mobile-run MOBILE_ARGS="--dart-define=API_BASE_URL=http://192.168.1.20:8000"
```

Для Wi-Fi замените пример реальным IP и добавьте его в `DJANGO_ALLOWED_HOSTS` в `.env`,
затем `make dev-up`. Разрешите подключение в локальном firewall; на iOS подтвердите
доступ к локальной сети. URL указывается **без** `/api/v1`: приложение добавляет путь.
HTTP и локальные медиа разрешены только в debug/local. Profile/release локальный режим
отклоняют; их требования HTTPS не ослаблены.

После `make up` можно проверить контракт настоящего API мобильным клиентом:

```bash
cd clients/iqro_mobile
IQRO_LOCAL_API_URL=http://127.0.0.1:8000 \
IQRO_MUSHAF_VISUAL_OUTPUT="$PWD/../../.dev/verification/mushafs" \
  flutter test test/integration/foundation_mushaf_live_test.dart
```

Тест проверяет все 13 записей каталога и реально рисует страницы 1, 42 и последнюю
каждого макета официальными ресурсами: 39 основных PNG и две страницы с редким символом IndoPak сохраняются в указанной папке. Проверяются
непустое изображение, ссылки аятов и границы слов. Без обеих переменных тест пропускается;
обычные unit/widget-тесты не требуют API и ключей. Полная загрузка всех страниц
для офлайна этим тестом не выполняется.

## Проверка проблем

- `make dev-doctor`: проверяет миграции, корпус, QF, ду’а, все настроенные переводы/тафсиры,
  полное аудио и готовность всех 13 макетов для обоих клиентов.
- Сбой сети или импорта: повторите `make up`. Готовые ресурсы останутся на месте;
  импорт продолжится с недостающих. Ошибка не выдаётся за успешную готовность.
- Повторный запуск готового окружения не обращается к QF и не запускает рендер.
  Ключи не нужны для чтения уже загруженного содержимого.
- Одновременные `make up`/`dev-data` одной рабочей копии блокируются локальным lock-файлом.
- Флаги `QF_*_SYNC_ENABLED` управляют периодическими задачами, а не первым импортом: для
  первого запуска достаточно ключей и корректного `QF_ENV`.
- `QF_CLIENT_ID/QF_CLIENT_SECRET` отсутствуют: заполните backend `.env` и повторите `dev-up`.
- Ошибка авторизации QF: проверьте окружение ключей и выданный доступ к Content Sync.
- Web работает, mobile пуст: обновите код приложения, проверьте его `API_BASE_URL` и `make dev-doctor`.
  Для первой загрузки шрифтов/изображений нужен доступ телефона к CDN.
- Телефон не соединяется: проверьте LAN IP, `DJANGO_ALLOWED_HOSTS`, firewall и порт 8000.
- Ошибка несовместимого существующего корпуса: импорт сохранил старую БД. Не удаляйте
  её и не обходите проверку — подготовьте отдельную миграцию соответствия данных.
  Обычный запуск сохраняет готовый старый корпус и не запускает его повторный импорт.
- Старый адрес страниц отвечает 410: обновите клиент; макеты QF теперь в `foundation/mushafs`, дополнительные растровые публикации — в `mushaf-renditions`.

Ни дамп коллеги, ни его папка `media/`, ни PDF не нужны для нового окружения.
Исторические поля и UUID старой БД сохраняются для прогресса, закладок и безопасного перехода.

Если образы уже собраны и исходники доступны через bind-mount, можно пропустить
повторную сборку: `make up DEV_UP_ARGS=--no-build`. Проверка и автозагрузка данных
при этом сохраняются. На новом компьютере без образов нужен обычный `make up`.

Низкоуровневые `docker compose up`, `npm run dev` и `flutter run` самостоятельно не
оркестрируют загрузку всего проекта. Командный вход после клонирования — `make up`
или `make mobile-run`; ручные миграции и `sync_*` в обычном первом запуске не нужны.
