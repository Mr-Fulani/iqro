# Мобильный веб-интерфейс: staging deploy 9 сентября 2026

Пользователь разрешил commit, merge и deploy после просмотра мобильной читалки.

## Исходники и слияние

- Ветка изменений: `codex/web-mobile-native-parity`, последний commit `e293e41`.
- Merge в текущую рабочую ветку `codex/mobile-ui-prototype`:
  `7e0d83817c875daccbaf942a075868c7e9d8badf`.
- Слияние прошло без конфликтов. Изменились только 23 файла в `services/web`.
- Git tree фронтенда после merge совпал с проверенной веткой:
  `731ddcef10bdcfb3a892621e104f8ed50ee83979`.
- Git remote в локальном репозитории не настроен; релиз доставлен штатным SSH/archive способом.

Изменения включают мобильное оформление, баннер, выбор языка в «Ещё», начальный режим
Мусхафа, свайпы, раскрываемые настройки и читалку с адаптацией к повороту экрана.
Прикладные обработчики данных, авторизации, синхронизации и воспроизведения сохранены.
Проверки реализации описаны в `services/web/docs/mobile-presentation-review.md`.

## Подготовка и резервная копия

Среда: `https://staging.iqro.forum`, существующий budget staging, `/opt/quran`.
Архив содержит только `services/web` из merge commit:

```text
source.tar SHA256:
c80d49e3ed7a11f44d84f891b887c6a23efbe8ffde40e9c387c45b158f19d3c8

BACKUP_FILE=/backups/quran_staging_20260909T055132Z.dump
backup SHA256:
881f2b634edcfe2da7d1e40b983daaa34b174148861036aa5bd7895c09d7c073
```

Новая копия размером около 132 MiB прошла проверку SHA256 и `pg_restore --list`.
Разрыв первой SSH-сессии не прервал создание завершённой копии; её наличие и результат
отдельной проверки подтверждены после повторного подключения.

Для backup применён runtime override `BACKUP_RETENTION_DAYS=0`: в штатном скрипте
удаление старых копий выполняется только при значении больше нуля. Совпадение SHA256
локального и серверного скрипта подтверждено перед запуском. Прежние копии и образы
сохранены; команды очистки не выполнялись.

## Развёртывание

Новый образ собран на сервере из отдельного каталога релиза. Docker build,
TypeScript и проверка целостности CSS прошли: 2 CSS-чанка, 114 460 байт.

```text
WEB_IMAGE=quran-platform-web:staging-7e0d838
Image ID=sha256:671e7f409424a11cd4798e00cea0f067e98a35e244f830314ae684735fdeb0eb
org.opencontainers.image.revision=7e0d83817c875daccbaf942a075868c7e9d8badf
Previous WEB_IMAGE=quran-platform-web:staging-37b5206
```

Переключён только `web` через существующие production/staging/budget Compose overlays,
с `up --no-deps --no-build --detach --wait web`. Контейнер достиг состояния `healthy`.
Исходники веба и `.deployed-commit` обновлены после успешных проверок.

ID и образы остальных восьми работающих сервисов совпали с исходным снимком.
Backend остался на `staging-c909c72`; миграции и публикация контента не выполнялись.
Проверка runtime budget прошла для всех девяти сервисов.

Журнал и материалы отката находятся в `/opt/quran/releases/web-7e0d838/`:
архив нового фронтенда, прежние исходники веба, прежний deployment marker,
снимки контейнеров, build/backup/verify/rollout/runtime-budget logs.
Для отката работающего веб-сервиса сохранён образ `staging-37b5206`; его можно
выбрать через `WEB_IMAGE` тем же точечным Compose-вызовом без обновления зависимостей.

## Проверки после деплоя

- API readiness: `status=ok`, database/cache/throttling — `true`.
- HTTP 200: `/ru`, `/ru/quran`, `/ru/dua`, `/ru/audio`, `/ar/quran`.
- В браузере загружена реальная страница Мусхафа с интерактивными областями аятов.
- При 390×844 область чтения занимает ровно весь viewport; изображение загружено,
  горизонтального переполнения нет.
- При 844×390 сохранена страница 1, изображение занимает ширину 844 px,
  доступна вертикальная прокрутка; горизонтального переполнения нет.
- Настройки открываются поверх страницы и остаются доступными после обратного поворота.
- После закрытия настроек возвращается читалка; временное изменение viewport сброшено.

Это обновление действующего staging. Новая production-среда не создавалась.
