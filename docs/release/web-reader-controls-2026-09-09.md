# Мобильная читалка: плеер выделенного аята, 9 сентября 2026

Дополнительные правки по запросу пользователя: подробные настройки вне читалки, компактное воспроизведение выбранного аята на текущей странице и окна перевода/прогресса в границах экрана. Ранее согласованный workflow commit → merge → staging deploy сохранён.

## Версия и проверки

- Ветка: `codex/web-reader-controls`, commit `13547a02a0c1f7d9d6028ee96be3be3169299239`.
- Merge в `codex/mobile-ui-prototype`: `84ff100b6d66925fbfec36ee5520f2f8720ff7ee`.
- Git tree `services/web` совпал с проверенной веткой: `674ad82b64d09b13b66285112e26c6de6c2b8868`.
- Изменены только 9 файлов `services/web`. Backend и нативный клиент в релиз не вошли.
- Lint, TypeScript, production build, CSS integrity и diff check прошли.
- Полный прогон 39/39 прошёл; после финальной привязки Media Session прошли 11/11 сценариев. Набор включает 40 уникальных проверок, из них 6 новых регрессий воспроизведения.
- Артефакты локальных проверок: `/private/tmp/iqro-mobile-review/reader-controls/`.

Отдельно проверены точные границы аята, пауза и возобновление, другая сура в настройках, смена страницы/выделения, запоздалые ответы API и метаданные медиа, отсутствие таймингов, системные кнопки и свёрнутая панель, сохранение скорости, desktop-плеер, поворот, сенсорные свайпы и клавиатура.

## Резервная копия и сборка

Среда: `https://staging.iqro.forum`, host `162.55.35.8`, каталог `/opt/quran`.

Архив только веб-исходников из merge commit:

```text
source.tar SHA256=6a3082fd59c6946a23e310586d9bf993e0623a88c073835b6f3fb42e26dfa74d
BACKUP_FILE=/backups/quran_staging_20260909T062426Z.dump
backup SHA256=16a350b25916512331d4cffdc77f4b753585ab7ac39fd7668490b396ea080f76
```

Копия около 132 MiB прошла checksum и `pg_restore --list`. Runtime override `BACKUP_RETENTION_DAYS=0` сохранил все прежние копии; совпадение серверного backup.sh с проверенным локальным скриптом подтверждено. Команд очистки не выполнялось.

Образ собран из отдельного каталога релиза после завершения передачи и успешной проверки SHA256. Ранняя проверка ещё передаваемого архива была отклонена по несовпадению checksum; этот архив не распаковывался и не использовался для сборки. Финальная сборка завершилась успешно: 2 CSS-чанка, 115 595 байт. Предсборочные предупреждения `ENOTFOUND backend` в каталоге ду’а связаны с изолированной Docker build-сетью; работа страницы проверена после запуска.

## Развёртывание и откат

```text
WEB_IMAGE=quran-platform-web:staging-84ff100
Image ID=sha256:a558bf524c661395e2273c7a01990112667b2411192165dcecebca8ce98da0bf
revision=84ff100b6d66925fbfec36ee5520f2f8720ff7ee
previous WEB_IMAGE=quran-platform-web:staging-7e0d838
```

В 06:31 UTC переключён только `web` через production/staging/budget overlays с `--no-deps --no-build --detach --wait`. Контейнер healthy. ID и образы остальных восьми работающих сервисов совпали со снимком перед релизом. Runtime budget verification прошёл для всех девяти сервисов.

После успешных HTTP-проверок обновлены только веб-исходники и `.deployed-commit`. Предыдущий образ, исходники и marker сохранены. Материалы находятся в `/opt/quran/releases/web-reader-controls-20260909/`: source archive, previous-web-source.tar, previous-deployed-commit, previous-web-image.txt, snapshots и журналы backup/verify/build/rollout/runtime-budget.

Для отката выбрать `WEB_IMAGE=quran-platform-web:staging-7e0d838` тем же точечным Compose-вызовом. Изменений схемы БД и миграций в релизе нет.

## Проверки после запуска

- API readiness `status=ok`, database/cache/throttling=true.
- HTTP 200: `/ru`, `/ru/quran`, `/ru/dua`, `/ru/audio`, `/ar/quran`.
- В реальном браузере при 390×844 загружена страница 1; выбран аят 1:2, компактная подпись соответствует выделению.
- Нажатие запускает «Звучит аят 1:2», кнопка меняется на паузу. Воспроизведение самостоятельно остановилось на позиции 17,94 с при общей длительности трека 53,31 с.
- Настройки открываются вне читалки, на экране доступна кнопка возврата.
- Окно перевода при 844×390 занимает 640×390, верхняя граница 0; контент прокручивается отдельно (clientHeight=329, scrollHeight=480). Горизонтального переполнения нет.
- После закрытия и поворота возвращается та же страница. Временный viewport override сброшен.

Настоящий Safari/iOS в проверку не входил. Localhost пересобран и оставлен работающим на порту 3101 с ограниченными API-фикстурами. Это обновление существующего staging.
